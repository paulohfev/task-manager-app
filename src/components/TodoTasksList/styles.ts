const styles = {
  cardContent: {
    alignItems: 'center',
    display: 'flex',
    justifyContent: 'space-between',
  },
  wrapper: {
    display: 'flex',
    flexDirection: 'column' as const,
    gap: '12px',
  }
};

export default styles;