const styles = {
  container: {
    alignItems: 'center',
    display: 'flex',
    justifyContent: 'center'
  },
  gridColumn: {
    display: 'flex',
    flexDirection: 'column' as const,
    gap: '12px',
  }
};

export default styles;